name: Docker Release

on:
  workflow_dispatch:
    inputs:
      version:
        description: 'Version tag (e.g., v1.0.0)'
        required: true
        default: 'v1.0.0'

permissions:
  contents: read

jobs:
  docker:
    runs-on: ubuntu-latest

    steps:
    - uses: actions/checkout@v4

    - name: Set up Docker Buildx
      uses: docker/setup-buildx-action@v3

    - name: Log in to Docker Hub
      uses: docker/login-action@v3
      with:
        username: hekaixin66-sketch
        password: ${{ secrets.DOCKERHUB_TOKEN }}

    - name: Build and push Docker image (AMD64)
      uses: docker/build-push-action@v5
      with:
        context: .
        file: ./Dockerfile
        push: true
        platforms: linux/amd64
        tags: |
          hekaixin66-sketch/xiaohongshuritter:${{ github.event.inputs.version }}
          hekaixin66-sketch/xiaohongshuritter:latest
        cache-from: type=gha
        cache-to: type=gha,mode=max

    - name: Build and push Docker image (ARM64)
      uses: docker/build-push-action@v5
      with:
        context: .
        file: ./Dockerfile.arm64
        push: true
        platforms: linux/arm64
        tags: |
          hekaixin66-sketch/xiaohongshuritter:${{ github.event.inputs.version }}-arm64
          hekaixin66-sketch/xiaohongshuritter:latest-arm64
        cache-from: type=gha
        cache-to: type=gha,mode=max